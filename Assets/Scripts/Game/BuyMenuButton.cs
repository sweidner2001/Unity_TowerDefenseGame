using TMPro;
using UnityEngine;

public class BuyMenu : MonoBehaviour
{

    public TMP_Text buyMenuText;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
